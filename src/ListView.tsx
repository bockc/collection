import React, { PureComponent } from 'react';
import { Text, View } from 'react-native';

const ListView = () => {
  return (
    <View
      style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: 'red' }}
    >
      <Text>LIST VIEW HEHE</Text>
    </View>
  );
};

export default ListView
